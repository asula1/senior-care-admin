# Server configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/seniorcare
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=seniorcare
spring.datasource.password=seniorcare123
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# H2 Console (disabled in production)
#spring.h2.console.enabled=false
#spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=seniorCareSecretKey2025SeniorWatchApp
jwt.expiration=86400000

# Logging Configuration
logging.level.org.springframework.web=INFO
logging.level.com.seniorcare=DEBUG

# WebSocket Configuration
websocket.allowed-origins=http://localhost:3000,http://localhost:8000,http://localhost:8080,http://localhost:8081,http://localhost:3001,https://seniorcare-admin.example.com,*
